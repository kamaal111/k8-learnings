apiVersion: v1
kind: Service
metadata:
  name: service1-entrypoint
  namespace: default
spec:
  type: NodePort
  selector:
    app: service1
  ports:
    - port: 8000
      targetPort: 8000
      nodePort: 30001
